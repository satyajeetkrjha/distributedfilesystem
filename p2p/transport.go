package p2p

// Peer interface represents remote node
type Peer interface {
}

// Transport is anything that handles the communication between nodes in the network and it can be of form TCP, UDP, Websockets
type Transport interface {
}
